<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artgallary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RateForApp"></activity>
        <activity android:name=".RatingBar1" />
        <activity android:name=".MarathilookPhotos" />
        <activity android:name=".BabyshowerPhotos" />
        <activity android:name=".ModelingPhotoes" />
        <activity android:name=".WeddingPhotoes" />
        <activity android:name=".AppointmentForm" />
        <activity android:name=".AllPreweddingPhotoes" />
        <activity android:name=".Babyphotos" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>